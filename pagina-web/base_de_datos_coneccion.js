/*
app.post('/ingre', function (solicitud, respuesta) {
    let user = solicitud.body.usuario;
    let password = solicitud.body.contraseña;

    /*let consulta = "INSERT INTO alumnos (nombre,edad) values (" + nombre + "," + edad + ");";
    conexion.query(consulta, function (resultado) {
        respuesta.send(resultado);
    });*/
   /* respuesta.send("datos_recibidos");
}) */